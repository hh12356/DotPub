# 复制成 .env 再填真值：cp .env.example .env
# .env 已被 .gitignore 忽略，真值永远只存在于本地和线上平台，不进仓库。
# 线上不读这个文件——环境变量由部署平台注入（systemd / Docker / 云 secrets），
# 且真实环境变量优先于本文件（见 core/setting.py 的 setdefault）。

# JWT 签名密钥。必须是密码学随机值，不要自己编：
#   py -c "import secrets; print(secrets.token_hex(32))"
# 泄露 = 任何人都能签发任意 user_id 的 token 直接冒充用户。换过密钥后所有旧 token 立即失效。
JWT_SECRET_KEY=填 secrets.token_hex(32) 的输出

# MySQL 密码
DB_PASSWORD=填你的数据库密码
